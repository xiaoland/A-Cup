openapi: 3.0.3
info:
  title: A-Cup API
  description: Minimal OpenAPI spec for A-Cup (example)
  version: 0.1.0
servers:
  - url: http://localhost:8787
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
